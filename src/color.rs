use {
    super::{tuple::tuple},
    crate::approx_eq::approxeq,
    std::ops,
};

#[derive(debug, clone, copy)]
pub struct color {
    red: f64,
    green: f64,
    blue: f64,
}

impl color {
    pub fn new(red: f64, green: f64, 
        self {red, green, blue}
    }
}


